'use client'

//import { signal } from '@preact/signals-react';

export default function Teste() {
  //const count = signal(0);

  return <>
    {/*<button onClick={() => count.value++}>{count}</button>
    {count}*/}
    <p className="text-red-500">Testando cor vermelha</p>
  </>
}